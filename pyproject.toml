[project]
name = "embedv2-agent-context"
version = "0.1.0"
description = "Count-free clinical-semantic context for EMBED data"
requires-python = ">=3.12,<3.14"
dependencies = []

[project.optional-dependencies]
mcp = [
    "mcp==2.0.0",
]

[dependency-groups]
dev = [
    "pyarrow==20.0.0",
]

[tool.uv]
package = false
